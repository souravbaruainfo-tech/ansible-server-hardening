#Global variables applied to all hosts
#Define all managed nodes
[all:vars]
ansible_user=oracle
ansible_ssh_private_key_fil=~/.ssh/id_rsa
ansible_connection=ssh
ansible_port=22

#RedHat-based systems
[RHEL]
redhat1.lab

#Debian-based systems
[Debian]
debian1.lab

#Group all managed nodes
[managed_nodes:children]
RHEL
Debian

#Define all managed nodes
[all:vars]


